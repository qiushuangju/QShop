import * as Api from '@/api/setting/delivery'

/**
 * 格式化分类列表
 */
export default {

    /**
     * 向服务端获取分类列表并格式化
     */
    getListSelect() {
        return new Promise((resolve, reject) => {

        })
    }

}